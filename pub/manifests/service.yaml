apiVersion: v1
kind: Service
metadata:
  name: pub
  labels:
    app: pub
spec:
  type: ClusterIP
  selector:
    app: pub
  ports:
    - protocol: TCP
      port: 5555
      targetPort: 5555
      name: pub
